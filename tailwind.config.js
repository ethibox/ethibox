module.exports = {
    purge: false,
    darkMode: 'class',
    theme: {
        maxHeight: {
            80: '80%',
        },
    },
    variants: {},
    plugins: [
        require('@tailwindcss/forms'), // eslint-disable-line
    ],
};
