module.exports = {
    purge: false,
    theme: {
        maxHeight: {
            80: '80%',
        },
    },
    variants: {},
    plugins: [
        require('@tailwindcss/ui'), // eslint-disable-line
    ],
};
